apiVersion: 1

datasources:
  - name: MongoDB
    type: grafana-mongodb-datasource
    access: proxy
    url: mongodb://mongodb:27017
    database: sensordata
    isDefault: true
    editable: true
    version: 1
    jsonData:
      timeout: 60